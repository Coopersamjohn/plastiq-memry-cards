import { environment } from "src/environments/environment";

export const hostname = environment.hostname;
export const port = environment.port;
export const flashcardServiceHostname = environment.flashcardServiceHostname;
export const flashcardServicePort = environment.flashcardServicePort;